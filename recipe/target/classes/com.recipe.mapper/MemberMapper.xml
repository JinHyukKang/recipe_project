<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.recipe.mapper.MemberMapper">
	
	<!-- 회원가입 -->
	<insert id="memberJoin">
       insert into users(user_id, user_pass, user_name, user_gender, user_birth, user_nickname, user_email, user_postcode,user_addr,user_detailaddr,user_extraaddr) 
       values(#{user_id}, #{user_pass}, #{user_name}, #{user_gender}, #{user_birth}, #{user_nickname}, #{user_email},#{user_postcode}, #{user_addr},#{user_detailaddr},#{user_extraaddr})
    </insert>
    
	<!-- 아이디 중복 체크 -->
	<select id="idChk" resultType="boolean">
		SELECT COUNT(*) FROM USERS
		WHERE user_id = #{user_id}
	</select>
	
	<!-- 로그인 -->
	<select id="MemberLogin" resultType="com.recipe.model.MemberVO">
	Select * from users where user_id=#{user_id} and user_pass=#{user_pass}
	</select>

</mapper>